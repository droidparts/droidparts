apply from: "$rootProject.projectDir/scripts/java_lib.gradle"

archivesBaseName = 'droidparts-support'

dependencies {
	compile project(':droidparts')
	compileOnly fileTree(dir: 'libs', include: '*.jar')
}
